#ifndef FUNCIONESUTN_H_
#define FUNCIONESUTN_H_

int suma(int numero1, int numero2);//realiza la suma de las cifras ingresadas
int resta(int numero1, int numero2);//realiza la resta de las cifras ingresadas
float division(float numero1, float numero2);//hace la division de ambas cifras y las devuelve en tipo float con 2 decimales
//si 0 es el divisor, muestra un mensaje diciendo que no se puede dividir por 0
int multiplicacion(int numero1, int numero2);//realiza la multiplicacion de ambas cifras
int factorial(int numero1);//saca el factorial
//si uno de los numeros que se factorea es un 0, se le otorga un 1 como resultado

#endif /* FUNCIONESUTN_H_ */
